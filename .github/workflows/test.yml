name: Test

# permissions:
#   id-token: write
#   contents: read

on:
  pull_request:
    branches: [main]

  push:
    branches:
      - master
      - "dev/*"
      - 'feature/*'

  workflow_dispatch:
    inputs:
      # project:
      #   description: Project
      #   type: environment
      #   required: true

      project:
        description: Name of the projec to do the thing on
        type: string
        required: true
        default: "dev"
      environment:
        description: The GitHub Environment to deploy to
        type: environment
        required: true
        default: 'Radio'
      somenumber:
        description: A number
        type: number
        required: true
      somebool:
        description: A boolean
        type: boolean
        required: true



jobs:
  test:
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    outputs:
      outputA: "hello"
      outputB: "goodbye"

    steps:
      - name: print
        run: echo "${{ inputs.project }}"
      - run: echo "${{ inputs.environment }}"
      - run: echo "${{ inputs.somenumber }}"
      - run: echo "${{ inputs.somebool }}"
